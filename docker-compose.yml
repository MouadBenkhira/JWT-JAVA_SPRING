services:
  db:
    image: postgres:15-alpine
    container_name: postgres_db
    environment:
      POSTGRES_DB: emplooyeeTimeTrackerDatabase
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5434:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d emplooyeeTimeTrackerDatabase"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: java_app
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/emplooyeeTimeTrackerDatabase
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
      SECRET: "0lpqMh8VLmyRaTFVT3dOtYa7VWCiwrJ7kfZuQrNb6MV"
    depends_on:
      db:
        condition: service_healthy
